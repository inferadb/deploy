apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gotk-components.yaml
  - gotk-sync.yaml
# Flux system components
# These files should be generated by `flux bootstrap` or `flux install`
# For development, these will be created during the bootstrap process
