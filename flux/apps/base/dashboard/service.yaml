apiVersion: v1
kind: Service
metadata:
  name: inferadb-dashboard
  namespace: inferadb
  labels:
    app.kubernetes.io/name: inferadb-dashboard
    app.kubernetes.io/component: dashboard
    app.kubernetes.io/part-of: inferadb
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: inferadb-dashboard
    app.kubernetes.io/component: dashboard
