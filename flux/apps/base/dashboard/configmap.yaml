apiVersion: v1
kind: ConfigMap
metadata:
  name: inferadb-dashboard-config
  namespace: inferadb
  labels:
    app.kubernetes.io/name: inferadb-dashboard
    app.kubernetes.io/component: dashboard
    app.kubernetes.io/part-of: inferadb
data:
  # API endpoint configuration
  INFERADB_API_URL: "http://inferadb-control.inferadb:9090"
  INFERADB_API_PUBLIC_URL: "http://localhost:9090"
